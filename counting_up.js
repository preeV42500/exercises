function sequence(number) {
  // starting at 1 up to and including number, push value onto result array
  var result = [];
  for (var i = 1; i <= number; i++) {
    result.push(i);
  }
  return result;
}
